using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace RunPythonScript
{
    class Program
    {
//        static void Main(string[] args)
//        {
//            // the python program as a string. Note '@' which allow us to have a multiline string
//            String prg = @"import sys
//x = int(sys.argv[1])
//y = int(sys.argv[2])
//print (x+y)";
//            StreamWriter sw = new StreamWriter("C:\\Users\\Abhijeet\\Desktop\\sum.py");
//            sw.Write(prg); // write this program to a file
//            sw.Close();

//            int a = 2;
//            int b = 2;

//            Process p = new Process(); // create process (i.e., the python program
//            p.StartInfo.FileName = "python.exe";
//            p.StartInfo.RedirectStandardOutput = true;
//            p.StartInfo.UseShellExecute = false; // make sure we can read the output from stdout
//            p.StartInfo.Arguments = "C:\\Users\\Abhijeet\\Desktop\\sum.py " + a + " " + b; // start the python program with two parameters
//            p.Start(); // start the process (the python program)
//            StreamReader s = p.StandardOutput;
//            String output = s.ReadToEnd();
//            string[] r = output.Split(new char[] { ' ' }); // get the parameter
//            Console.WriteLine(r[0]);
//            p.WaitForExit();

//            Console.ReadLine(); // wait for a key press
//        }
    }
}
